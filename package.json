{
  "name": "better-og-workspace",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --filter better-og build",
    "clean": "pnpm --filter better-og clean && rm -rf docs/.next docs/tsconfig.tsbuildinfo docs/tsconfig.typecheck.tsbuildinfo",
    "typecheck": "pnpm --filter better-og typecheck && pnpm exec tsc -b docs/tsconfig.typecheck.json --pretty",
    "check": "ultracite check",
    "fix": "ultracite fix",
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "lefthook": "^2.1.1",
    "oxfmt": "^0.35.0",
    "oxlint": "^1.50.0",
    "typescript": "^5.9.0",
    "ultracite": "7.2.4"
  },
  "engines": {
    "node": ">=22.20.0",
    "pnpm": ">=10.29.1"
  },
  "packageManager": "pnpm@10.29.1"
}
