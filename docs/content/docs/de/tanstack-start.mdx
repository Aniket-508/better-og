---
title: TanStack Start
description: better-og mit TanStack Start verwenden.
---

## Uberblick

Mit TanStack Start kannst du nahezu dieselben OG-Komponenten und Font-Setups wie in Next.js weiterverwenden.

## Beispiel

```tsx title="app/og/[lang]/route.tsx"
import { resolveFontSetup } from "@better-og/core";
import {
  createOgRouteHandler,
  loadGoogleFontForImageResponse,
} from "@better-og/tanstack-start";

export const runtime = "nodejs";

const fontSetup = await resolveFontSetup({
  baseFonts: await loadGoogleFontForImageResponse({
    family: "Geist",
    weights: [400, 700],
  }),
});

export const GET = createOgRouteHandler({
  component: (
    <div
      style={{
        width: "100%",
        height: "100%",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontFamily: fontSetup.families.base,
      }}
    >
      Platform-aware OG
    </div>
  ),
  baseFonts: fontSetup.fonts,
});
```

## Hinweise

- Der Adapter unterstutzt sowohl Params als auch `localeFromRequest` mit Takumi auf Node.
