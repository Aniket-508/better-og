---
title: Next.js (Node)
description: Usare better-og con Next.js sul runtime Node.
---

## Panoramica

`@better-og/next` riunisce font, proxy e preset per creare route OG consapevoli della piattaforma direttamente in Next.js.

## Esempio

```tsx title="app/og/[lang]/route.tsx"
import { resolveFontSetup } from "@better-og/core";
import {
  createOgRouteHandler,
  loadGoogleFontForImageResponse,
} from "@better-og/next";

export const runtime = "nodejs";

const fontSetup = await resolveFontSetup({
  baseFonts: await loadGoogleFontForImageResponse({
    family: "Geist",
    weights: [400, 700],
  }),
});

export const GET = createOgRouteHandler({
  component: (
    <div
      style={{
        width: "100%",
        height: "100%",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontFamily: fontSetup.families.base,
      }}
    >
      Platform-aware OG
    </div>
  ),
  baseFonts: fontSetup.fonts,
});
```

## Note

- Il renderer predefinito e `next/og`.
- Passa a `renderer: "takumi"` quando ti serve.
