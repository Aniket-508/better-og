---
title: Next.js (Node)
description: Использование better-og с Next.js на Node runtime.
---

## Обзор

`@better-og/next` объединяет шрифты, proxy и presets, чтобы упростить OG routes прямо внутри Next.js.

## Пример

```tsx title="app/og/[lang]/route.tsx"
import { resolveFontSetup } from "@better-og/core";
import {
  createOgRouteHandler,
  loadGoogleFontForImageResponse,
} from "@better-og/next";

export const runtime = "nodejs";

const fontSetup = await resolveFontSetup({
  baseFonts: await loadGoogleFontForImageResponse({
    family: "Geist",
    weights: [400, 700],
  }),
});

export const GET = createOgRouteHandler({
  component: (
    <div
      style={{
        width: "100%",
        height: "100%",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontFamily: fontSetup.families.base,
      }}
    >
      Platform-aware OG
    </div>
  ),
  baseFonts: fontSetup.fonts,
});
```

## Заметки

- Рендерер по умолчанию — `next/og`.
- При необходимости можно перейти на `renderer: "takumi"`.
