---
title: TanStack Start
description: TanStack Start ile better-og kullanimi.
---

## Genel bakis

TanStack Start, Next.js projelerine benzer sekilde ayni OG component ve font kurulumunu yeniden kullanabilir.

## Ornek

```tsx title="app/og/[lang]/route.tsx"
import { resolveFontSetup } from "@better-og/core";
import {
  createOgRouteHandler,
  loadGoogleFontForImageResponse,
} from "@better-og/tanstack-start";

export const runtime = "nodejs";

const fontSetup = await resolveFontSetup({
  baseFonts: await loadGoogleFontForImageResponse({
    family: "Geist",
    weights: [400, 700],
  }),
});

export const GET = createOgRouteHandler({
  component: (
    <div
      style={{
        width: "100%",
        height: "100%",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontFamily: fontSetup.families.base,
      }}
    >
      Platform-aware OG
    </div>
  ),
  baseFonts: fontSetup.fonts,
});
```

## Notlar

- Adaptor, Node uzerinde Takumi ile hem params hem `localeFromRequest` destekler.
