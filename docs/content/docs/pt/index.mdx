---
title: Introducao
description: Renderizacao Open Graph consciente da plataforma com layout adaptativo e fontes multilingues.
---

## Visao geral

better-og e um toolkit para imagens Open Graph que ajusta automaticamente o tamanho e o layout conforme a plataforma que pede a imagem.

- Deteccao de plataforma com sinais reais do request.
- Proporcoes e safe areas prontas a usar.
- Adaptadores para Next.js, Workers e TanStack Start.

## Instalacao

Instale apenas os pacotes do runtime alvo e reutilize o mesmo componente OG em qualquer ambiente.

```bash tab="Next.js (next/og)"
pnpm add @better-og/core next react
```

```bash tab="Next.js + Takumi"
pnpm add @better-og/core @takumi-rs/image-response next react
```

```bash tab="Edge / Workers"
pnpm add @better-og/core @takumi-rs/image-response @takumi-rs/wasm next react
```

```bash tab="TanStack Start"
pnpm add @better-og/core @takumi-rs/image-response react
```

## Primeira rota

Crie um handler OG, carregue as fontes e devolva o componente que deve ser renderizado como imagem.

```tsx title="app/og/[lang]/route.tsx"
import { resolveFontSetup } from "@better-og/core";
import {
  createOgRouteHandler,
  loadGoogleFontForImageResponse,
} from "@better-og/next";

export const runtime = "nodejs";
export const revalidate = false;

const fontSetup = await resolveFontSetup({
  baseFonts: await loadGoogleFontForImageResponse({
    family: "Geist",
    weights: [400, 700],
  }),
  fallbackLocales: ["ja"],
});

export const GET = createOgRouteHandler({
  component: (ogContext) => (
    <div
      style={{
        display: "flex",
        width: "100%",
        height: "100%",
        alignItems: "center",
        justifyContent: "center",
        fontFamily: fontSetup.families.base,
        paddingBottom: 32 + ogContext.safeArea.bottom,
        background: "linear-gradient(135deg, #0d192c, #174a70, #4993d6)",
        color: "white",
      }}
    >
      <div style={{ fontSize: 64, fontWeight: 700 }}>Hello from better-og</div>
    </div>
  ),
  baseFonts: fontSetup.fonts,
});
```

## Adicionar o proxy

Adicione `withOgRewrite` para preencher automaticamente `platform`, `layout` e `aspect_ratio`.

```ts title="proxy.ts"
import { withOgRewrite } from "@better-og/next";
import type { NextRequest } from "next/server";

export async function proxy(request: NextRequest) {
  return await withOgRewrite(request);
}

export const config = {
  matcher: ["/og/:path*"],
};
```

## Configuracao do Next.js

Se usar Takumi em Node, mantenha o pacote como externo no Next.js.

```ts title="next.config.ts"
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  serverExternalPackages: ["@takumi-rs/image-response"],
};

export default nextConfig;
```
