---
title: Next.js (Node)
description: Використання better-og з Next.js на Node runtime.
---

## Огляд

`@better-og/next` поєднує шрифти, proxy та presets, щоб спростити platform-aware OG routes у Next.js.

## Приклад

```tsx title="app/og/[lang]/route.tsx"
import { resolveFontSetup } from "@better-og/core";
import {
  createOgRouteHandler,
  loadGoogleFontForImageResponse,
} from "@better-og/next";

export const runtime = "nodejs";

const fontSetup = await resolveFontSetup({
  baseFonts: await loadGoogleFontForImageResponse({
    family: "Geist",
    weights: [400, 700],
  }),
});

export const GET = createOgRouteHandler({
  component: (
    <div
      style={{
        width: "100%",
        height: "100%",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontFamily: fontSetup.families.base,
      }}
    >
      Platform-aware OG
    </div>
  ),
  baseFonts: fontSetup.fonts,
});
```

## Нотатки

- Рендерер за замовчуванням — `next/og`.
- За потреби можна перейти на `renderer: "takumi"`.
