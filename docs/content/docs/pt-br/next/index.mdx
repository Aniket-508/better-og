---
title: Next.js (Node)
description: Usar better-og com Next.js no runtime Node.
---

## Visao geral

`@better-og/next` junta fontes, proxy e presets para construir rotas OG conscientes da plataforma direto no Next.js.

## Exemplo

```tsx title="app/og/[lang]/route.tsx"
import { resolveFontSetup } from "@better-og/core";
import {
  createOgRouteHandler,
  loadGoogleFontForImageResponse,
} from "@better-og/next";

export const runtime = "nodejs";

const fontSetup = await resolveFontSetup({
  baseFonts: await loadGoogleFontForImageResponse({
    family: "Geist",
    weights: [400, 700],
  }),
});

export const GET = createOgRouteHandler({
  component: (
    <div
      style={{
        width: "100%",
        height: "100%",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontFamily: fontSetup.families.base,
      }}
    >
      Platform-aware OG
    </div>
  ),
  baseFonts: fontSetup.fonts,
});
```

## Notas

- O renderer padrao e `next/og`.
- Voce pode mudar para `renderer: "takumi"` quando precisar.
